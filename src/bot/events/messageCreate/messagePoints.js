////   DEPENDENCIAS  ////
const PATH = require("path");
const { getCommands, createAnswer } = require(PATH.resolve("src","bot","utilities","utilities.js"));
require("dotenv").config()
////   INICIO CODIGO ////
module.exports = async (Client, Interaction) => {
    
}